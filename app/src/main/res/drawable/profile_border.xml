<!-- image_border.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent" /> <!-- 배경 색 (투명) -->
    <stroke
        android:width="6dp"
        android:color="@color/game_light_brown" /> <!-- 테두리 색상 -->
</shape>
